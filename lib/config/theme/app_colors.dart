import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF2196F3);
  static const Color primaryLight = Color(0xFF64B5F6);
  static const Color primaryDark = Color(0xFF1976D2);

  // Secondary Colors
  static const Color secondary = Color(0xFF03DAC6);
  static const Color secondaryLight = Color(0xFF64FFDA);
  static const Color secondaryDark = Color(0xFF018786);

  // Surface Colors
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFF5F5F5);
  static const Color surfaceDark = Color(0xFFE0E0E0);

  // Background Colors
  static const Color background = Color(0xFFF5F5F5);
  static const Color backgroundLight = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFFE0E0E0);

  // Text Colors
  static const Color textPrimary = Color(0xFF212121);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFF9E9E9E);

  // Status Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color error = Color(0xFFE53935);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Color(0xFF29B6F6);

  // Border Colors
  static const Color border = Color(0xFFE0E0E0);
  static const Color borderLight = Color(0xFFF5F5F5);
  static const Color borderDark = Color(0xFFBDBDBD);

  // Overlay Colors
  static const Color overlay = Color(0x80000000);
  static const Color overlayLight = Color(0x40000000);
  static const Color overlayDark = Color(0xCC000000);

  // Shadow Colors
  static const Color shadow = Color(0xFF000000);
  static const Color shadowLight = Color(0x1A000000);
  static const Color shadowDark = Color(0x33000000);

  // Disabled Colors
  static const Color disabled = Color(0xFFBDBDBD);
  static const Color disabledLight = Color(0xFFE0E0E0);
  static const Color disabledDark = Color(0xFF9E9E9E);

  // Divider Colors
  static const Color divider = Color(0xFFE0E0E0);
  static const Color dividerLight = Color(0xFFF5F5F5);
  static const Color dividerDark = Color(0xFFBDBDBD);

  // Selection Colors
  static const Color selection = Color(0xFFE3F2FD);
  static const Color selectionLight = Color(0xFFF5F9FF);
  static const Color selectionDark = Color(0xFFBBDEFB);

  // Ripple Colors
  static const Color ripple = Color(0x1A000000);
  static const Color rippleLight = Color(0x0D000000);
  static const Color rippleDark = Color(0x33000000);

  // Focus Colors
  static const Color focus = Color(0xFF2196F3);
  static const Color focusLight = Color(0xFF64B5F6);
  static const Color focusDark = Color(0xFF1976D2);

  // Highlight Colors
  static const Color highlight = Color(0xFFE3F2FD);
  static const Color highlightLight = Color(0xFFF5F9FF);
  static const Color highlightDark = Color(0xFFBBDEFB);

  // Splash Colors
  static const Color splash = Color(0x1A000000);
  static const Color splashLight = Color(0x0D000000);
  static const Color splashDark = Color(0x33000000);

  // Hover Colors
  static const Color hover = Color(0xFFF5F5F5);
  static const Color hoverLight = Color(0xFFFAFAFA);
  static const Color hoverDark = Color(0xFFEEEEEE);

  // Active Colors
  static const Color active = Color(0xFFE3F2FD);
  static const Color activeLight = Color(0xFFF5F9FF);
  static const Color activeDark = Color(0xFFBBDEFB);

  // Inactive Colors
  static const Color inactive = Color(0xFFE0E0E0);
  static const Color inactiveLight = Color(0xFFF5F5F5);
  static const Color inactiveDark = Color(0xFFBDBDBD);

  // Selected Colors
  static const Color selected = Color(0xFFE3F2FD);
  static const Color selectedLight = Color(0xFFF5F9FF);
  static const Color selectedDark = Color(0xFFBBDEFB);

  // Unselected Colors
  static const Color unselected = Color(0xFFE0E0E0);
  static const Color unselectedLight = Color(0xFFF5F5F5);
  static const Color unselectedDark = Color(0xFFBDBDBD);

  // Disabled Text Colors
  static const Color disabledText = Color(0xFF9E9E9E);
  static const Color disabledTextLight = Color(0xFFBDBDBD);
  static const Color disabledTextDark = Color(0xFF757575);

  // Disabled Icon Colors
  static const Color disabledIcon = Color(0xFF9E9E9E);
  static const Color disabledIconLight = Color(0xFFBDBDBD);
  static const Color disabledIconDark = Color(0xFF757575);

  // Disabled Border Colors
  static const Color disabledBorder = Color(0xFFE0E0E0);
  static const Color disabledBorderLight = Color(0xFFF5F5F5);
  static const Color disabledBorderDark = Color(0xFFBDBDBD);

  // Disabled Background Colors
  static const Color disabledBackground = Color(0xFFF5F5F5);
  static const Color disabledBackgroundLight = Color(0xFFFAFAFA);
  static const Color disabledBackgroundDark = Color(0xFFEEEEEE);
}
